# Storage Directory

This directory contains runtime data generated by the application.

```
storage/
├── cache/     # Application cache files
├── logs/      # Log files
└── views/     # Compiled view templates
```

**Note:** This directory should be writable by the web server. The contents (except `.gitkeep` files) should be in `.gitignore`.
