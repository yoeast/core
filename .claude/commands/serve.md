---
description: Start the Core development server
---

Start the HTTP server with hot reload for development:

```bash
bun run cli.ts serve
```

The server will start on the port defined in `.env` (default: 3000).
